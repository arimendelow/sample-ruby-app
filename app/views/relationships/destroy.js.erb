/*
'follow_form' is a div that wraps the form, not the form itself.
This syntax, inspired by CSS, uses the # symbol to indicate a CSS id.
The second method, html, updates the HTML inside the relevant element
with the contents of its argument.
The escape_javascript method is needed to escape out the result
when inserting HTML in a JavaScript file.
*/
$("#follow_form").html("<%= escape_javascript(render('users/follow')) %>");
$("#followers").html('<%= @user.followers.count %>');
