<% provide(:title, "Sign Up") %>

<!-- For the form -->
<% provide(:submit_url, signup_path) %>
<% provide(:button_text, "Make my account") %>

<h1>Sign up!</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <!-- Render the form template partial -->
    <%= render 'form' %>
  </div>
</div>
